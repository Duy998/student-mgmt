index.html
    Hệ thống cần biết bạn đã đăng nhập hay chưa để hiển thị đúng trang phù hợp:
        Nếu bạn đã đăng nhập → vào trang chính (Dashboard).
        Nếu bạn chưa đăng nhập → chuyển đến trang đăng nhập (Login).

    Call func getToken() in the file config.js

login.html
    Hệ thống cần biết bạn đã đăng nhập hay chưa để hiển thị đúng trang phù hợp:
        Nếu bạn đã đăng nhập → vào trang chính (Dashboard).
        Nếu bạn chưa đăng nhập → chuyển đến trang đăng nhập (Login).

    Call func loginForm in the file auth.js


    